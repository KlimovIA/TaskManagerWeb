{
  "name": "kanban-desk-web",
  "version": "1.0.0",
  "description": "Kanban-доска для управления задачами",
  "scripts": {
    "predev": "node scripts/check-env.js",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  }
}
