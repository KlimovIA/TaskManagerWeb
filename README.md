# Kanban Desk Web

Канбан-доска для управления задачами с иерархической структурой: **Проекты → Задачи → Подзадачи**.

## 🚀 Возможности

- **Управление проектами** — создание, редактирование, удаление проектов
- **Управление задачами** — задачи со статусами (активные, завершённые, на доработке)
- **Канбан-доска** — настраиваемые этапы для каждой задачи
- **Подзадачи** — с типами (feature, bug, enhancement, docs, refactor, test, ci, security, performance, design, translation)
- **Заметки** — заметки к подзадачам
- **История операций** — отслеживание изменений для каждой подзадачи
- **Drag-and-drop** — перемещение подзадач между этапами

## 🛠 Технологии

- TypeScript
- Vite
- IndexedDB (локальное хранение данных)
- Чистый CSS (без фреймворков)

## 📦 Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/USERNAME/REPO.git

# Перейдите в директорию проекта
cd REPO

# Установите зависимости
npm install
```

## 🚀 Запуск

```bash
# Запуск dev-сервера (автоматическая проверка зависимостей)
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен-сборки
npm run preview
```

## 🌐 Развёртывание на GitHub Pages

Проект настроен для автоматического развёртывания через GitHub Actions.

### Настройка

1. Запушьте репозиторий на GitHub
2. В настройках репозитория: **Settings → Pages**
3. **Source**: GitHub Actions
4. Workflow автоматически запустится при пуше в ветку `main` или `master`

Сайт будет доступен по адресу:
```
https://USERNAME.github.io/REPO/
```

## 📁 Структура проекта

```
├── src/
│   ├── main.ts              # Точка входа
│   ├── types/models.ts      # Типы данных
│   ├── storage/
│   │   ├── database.ts      # Работа с IndexedDB
│   │   └── boardService.ts  # Бизнес-логика
│   ├── ui/                  # UI-компоненты
│   │   ├── projectList.ts
│   │   ├── taskList.ts
│   │   ├── stageRenderer.ts
│   │   ├── taskPanel.ts
│   │   └── historyPanel.ts
│   └── styles/main.css      # Стили
├── index.html               # HTML-шаблон
├── vite.config.ts           # Конфигурация Vite
├── tsconfig.json            # Конфигурация TypeScript
└── package.json             # Зависимости и скрипты
```

## 📝 Лицензия

MIT
